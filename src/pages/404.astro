---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - Página no encontrada">
  <section class="error-section">
    <div class="container">
      <div class="error-content fade-in-up">
        <!-- Animated 404 -->
        <div class="error-code">
          <span class="digit">4</span>
          <span class="digit vinyl-spin">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" class="vinyl-icon">
              <!-- Vinyl Record -->
              <circle cx="100" cy="100" r="90" fill="#1a1a1a"/>
              <circle cx="100" cy="100" r="85" fill="#0a0a0a"/>
              
              <!-- Grooves -->
              <circle cx="100" cy="100" r="75" fill="none" stroke="#2a2a2a" stroke-width="1"/>
              <circle cx="100" cy="100" r="65" fill="none" stroke="#2a2a2a" stroke-width="1"/>
              <circle cx="100" cy="100" r="55" fill="none" stroke="#2a2a2a" stroke-width="1"/>
              <circle cx="100" cy="100" r="45" fill="none" stroke="#2a2a2a" stroke-width="1"/>
              
              <!-- Label -->
              <circle cx="100" cy="100" r="35" fill="#8b5cf6"/>
              <circle cx="100" cy="100" r="30" fill="#7c3aed"/>
              
              <!-- Center hole -->
              <circle cx="100" cy="100" r="8" fill="#0a0a0a"/>
            </svg>
          </span>
          <span class="digit">4</span>
        </div>

        <!-- Error Message -->
        <h1 class="error-title">Página no encontrada</h1>
        <p class="error-description">
          Parece que esta pista no existe en nuestra biblioteca. 
          El contenido que buscas puede haber sido movido o eliminado.
        </p>

        <!-- Action Buttons -->
        <div class="error-actions">
          <a href="/" class="btn btn-primary">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
            Volver al inicio
          </a>
          <a href="/ep" class="btn btn-outline">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/>
              <path d="M19 10v2a7 7 0 0 1-14 0v-2"/>
              <line x1="12" x2="12" y1="19" y2="22"/>
              <line x1="8" x2="16" y1="22" y2="22"/>
            </svg>
            Ver episodios
          </a>
        </div>

        <!-- Suggestions -->
        <div class="suggestions">
          <p class="suggestions-title">También puedes explorar:</p>
          <div class="suggestions-links">
            <a href="/blog">Blog</a>
            <span class="separator">•</span>
            <a href="/about">Equipo</a>
            <span class="separator">•</span>
            <a href="/calendario">Calendario</a>
            <span class="separator">•</span>
            <a href="/contacto">Contacto</a>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .error-section {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-2xl) 0;
  }

  .container {
    max-width: 800px;
    text-align: center;
  }

  .error-content {
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  /* 404 Code */
  .error-code {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: var(--spacing-xl);
    font-size: 8rem;
    font-weight: 800;
    font-family: var(--font-display);
    line-height: 1;
  }

  .digit {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .vinyl-spin {
    display: inline-block;
    width: 8rem;
    height: 8rem;
    animation: spin 3s linear infinite;
  }

  .vinyl-icon {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 10px 30px rgba(139, 92, 246, 0.3));
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Text */
  .error-title {
    font-size: 2.5rem;
    margin-bottom: var(--spacing-md);
    color: var(--color-text-main);
  }

  .error-description {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: var(--spacing-2xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Actions */
  .error-actions {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: var(--spacing-2xl);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding: 0.875rem 1.75rem;
    font-size: 1rem;
  }

  /* Suggestions */
  .suggestions {
    padding-top: var(--spacing-xl);
    border-top: 1px solid var(--color-border);
  }

  .suggestions-title {
    color: var(--color-text-muted);
    font-size: 0.95rem;
    margin-bottom: var(--spacing-md);
  }

  .suggestions-links {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

  .suggestions-links a {
    color: var(--color-primary);
    font-weight: 500;
    transition: color 0.2s;
  }

  .suggestions-links a:hover {
    color: var(--color-secondary);
    text-decoration: underline;
  }

  .separator {
    color: var(--color-text-dim);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .error-code {
      font-size: 5rem;
      gap: 0.5rem;
    }

    .vinyl-spin {
      width: 5rem;
      height: 5rem;
    }

    .error-title {
      font-size: 2rem;
    }

    .error-description {
      font-size: 1rem;
    }

    .error-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
