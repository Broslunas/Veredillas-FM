---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container footer-content">
    <div class="footer-brand">
      <h3><span class="text-gradient">Veredillas FM</span></h3>
      <p>La voz de nuestra escuela, directo a tus oídos.</p>
    </div>
    <div class="footer-links">
      <div class="link-group">
        <h4>Explorar</h4>
        <a href="/" class="hover-link">Inicio</a>
        <a href="/blog" class="hover-link">Blog</a>
        <a href="/ep" class="hover-link">EP</a>
        <a href="/about" class="hover-link">Equipo</a>
      </div>
      <div class="link-group">
        <h4>Social</h4>
        <a href="#" class="hover-link">Instagram</a>
        <a href="#" class="hover-link">Twitter</a>
        <a href="#" class="hover-link">Spotify</a>
      </div>
    </div>
  </div>
  <div class="container copyright">
    <p>&copy; {currentYear} Veredillas FM. Todos los derechos reservados.</p>
    <p class="credits">
        Creado por <a href="https://broslunas.com" target="_blank" rel="noopener noreferrer" class="creator-link">Broslunas</a>
    </p>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    padding: var(--spacing-2xl) 0 var(--spacing-xl);
    margin-top: var(--spacing-2xl);
    position: relative;
    overflow: hidden;
  }
  
  /* Add a subtle glow at the bottom */
  .footer::after {
    content: '';
    position: absolute;
    bottom: -50%;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 50%;
    background: radial-gradient(circle, var(--color-primary-glow) 0%, transparent 70%);
    opacity: 0.2;
    filter: blur(80px);
    pointer-events: none;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-2xl);
    margin-bottom: var(--spacing-xl);
    position: relative;
    z-index: 1;
  }

  .footer-brand h3 {
    font-size: 1.5rem;
    margin-bottom: var(--spacing-sm);
  }

  .footer-brand p {
    color: var(--color-text-muted);
    max-width: 300px;
    font-size: 0.95rem;
  }

  .footer-links {
    display: flex;
    gap: var(--spacing-2xl);
    justify-content: flex-end;
  }

  .link-group h4 {
    margin-bottom: var(--spacing-md);
    color: var(--color-text-main);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .link-group a {
    display: block;
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-sm);
    transition: all 0.2s ease;
  }

  .link-group a:hover {
    color: var(--color-primary);
    transform: translateX(4px);
  }

  .copyright {
    position: relative;
    z-index: 1;
    color: var(--color-text-dim);
    font-size: 0.875rem;
    text-align: center;
    border-top: 1px solid rgba(255,255,255,0.05);
    padding-top: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }
  
  .credits {
      font-size: 0.8rem;
      color: var(--color-text-dim);
      opacity: 0.8;
  }

  .creator-link {
      color: var(--color-text-muted);
      font-weight: 500;
      transition: color 0.2s ease;
      text-decoration-thickness: 1px;
      text-underline-offset: 2px;
  }

  .creator-link:hover {
      color: #3b82f6; /* Broslunas Brand Color roughly (blue) or just primary */
      text-decoration: underline;
  }
  
  [data-theme="light"] .copyright {
      border-top-color: rgba(0,0,0,0.05);
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .footer-links {
      justify-content: center;
      gap: var(--spacing-xl);
    }
    
    .footer-brand p {
        margin: 0 auto;
    }
  }
</style>
